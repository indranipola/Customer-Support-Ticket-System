<Window x:Class="TicketSystem.WPF.Views.CreateTicketView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Ticket" Height="350" Width="400">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Subject -->
        <TextBlock Text="Subject:" Grid.Row="0" Grid.Column="0" Margin="5"/>
        <TextBox Grid.Row="0" Grid.Column="1" Margin="5"
                 Text="{Binding Subject}" />

        <!-- Description -->
        <TextBlock Text="Description:" Grid.Row="1" Grid.Column="0" Margin="5"/>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="5"
                 Height="100"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 Text="{Binding Description}" />

        <!-- Priority -->
        <TextBlock Text="Priority:" Grid.Row="2" Grid.Column="0" Margin="5"/>
        <ComboBox Grid.Row="2" Grid.Column="1" Margin="5"
                  SelectedItem="{Binding Priority}">
            <ComboBoxItem Content="Low"/>
            <ComboBoxItem Content="Medium"/>
            <ComboBoxItem Content="High"/>
        </ComboBox>

        <!-- Submit Button -->
        <Button Content="Submit Ticket"
                Grid.Row="3"
                Grid.ColumnSpan="2"
                Margin="5"
                Height="35"
                Command="{Binding CreateTicketCommand}" />
    </Grid>

</Window>
